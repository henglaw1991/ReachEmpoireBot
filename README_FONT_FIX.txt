ReachEmpireBot Font Fixed Patch

This PATCH contains the corrected HTML/CSS files only.
How to use:
1. Extract this ZIP.
2. Copy/overwrite these files into your existing website folder, keeping the same paths.
3. Press Ctrl + F5 in browser.

Fix applied:
- Removed inconsistent font-family rules from CSS.
- Removed inline font-family where possible.
- Injected global Poppins override into every HTML page.
- Added standard-font.css in the CSS folder.

Standard font: Poppins.
